<UserControl x:Class="ActionOrbit.App.Views.Actions.RingPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.Name="Canlı halka önizlemesi">
    <Expander Header="Canlı halka önizlemesi"
              IsExpanded="False"
              ToolTip="Halkanın tüm sayfalarını gör ve aksiyonları görsel olarak düzenle">
        <Border Margin="0,8,0,0"
                Padding="12"
                Background="{DynamicResource SoftSurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold"
                                   Text="{Binding RingPreviewSummary}" />
                        <TextBlock Margin="0,3,12,0"
                                   Style="{DynamicResource MutedText}"
                                   Text="Düğmeye tıklayarak seç; sürükleyerek sırala."
                                   TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Grid.Column="1"
                                HorizontalAlignment="Right"
                                Visibility="{Binding HasRingPreviewPages, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock HorizontalAlignment="Right"
                                   Style="{DynamicResource TinyLabel}"
                                   Text="SAYFA" />
                        <TextBlock Margin="0,3,0,0"
                                   HorizontalAlignment="Right"
                                   FontWeight="SemiBold"
                                   Text="{Binding RingPreviewPageText}" />
                    </StackPanel>
                </Grid>

                <Canvas Grid.Row="1"
                        Width="232"
                        Height="232"
                        Margin="0,8,0,0"
                        HorizontalAlignment="Center">
                    <Ellipse Canvas.Left="38"
                             Canvas.Top="38"
                             Width="156"
                             Height="156"
                             Stroke="{DynamicResource BorderBrush}"
                             StrokeDashArray="2,4"
                             StrokeThickness="1" />

                    <Border Canvas.Left="87"
                            Canvas.Top="87"
                            Width="58"
                            Height="58"
                            Background="{DynamicResource PrimaryActionBrush}"
                            BorderBrush="{DynamicResource PrimaryActionBrush}"
                            BorderThickness="1"
                            CornerRadius="29">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock HorizontalAlignment="Center"
                                       FontSize="11"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource OnPrimaryActionBrush}"
                                       Text="AO" />
                            <TextBlock HorizontalAlignment="Center"
                                       FontSize="8"
                                       Foreground="{DynamicResource OnPrimaryActionBrush}"
                                       Opacity="0.78"
                                       Text="MERKEZ" />
                        </StackPanel>
                    </Border>

                    <ItemsControl Width="232"
                                  Height="232"
                                  ItemsSource="{Binding RingPreviewSlots}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding Left}" />
                                <Setter Property="Canvas.Top" Value="{Binding Top}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Width="58"
                                        Height="58"
                                        Margin="0"
                                        Padding="0"
                                        AllowDrop="True"
                                        AutomationProperties.Name="{Binding Title}"
                                        AutomationProperties.HelpText="Seçmek için tıkla; sıralamak için sürükle."
                                        Command="{Binding DataContext.ActionEditor.SelectRingPreviewSlotCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        PreviewMouseLeftButtonDown="RingSlot_PreviewMouseLeftButtonDown"
                                        PreviewMouseMove="RingSlot_PreviewMouseMove"
                                        DragOver="RingSlot_DragOver"
                                        Drop="RingSlot_Drop"
                                        KeyDown="RingSlot_KeyDown">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Cursor" Value="Hand" />
                                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                                            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource SelectedBorderBrush}" />
                                            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                                            <Setter Property="RenderTransform">
                                                <Setter.Value>
                                                    <ScaleTransform ScaleX="1" ScaleY="1" />
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="RingShell"
                                                                Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="1"
                                                                CornerRadius="29">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                              VerticalAlignment="Center" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="RingShell" Property="BorderBrush" Value="{DynamicResource PrimaryActionBrush}" />
                                                                <Setter Property="RenderTransform">
                                                                    <Setter.Value>
                                                                        <ScaleTransform ScaleX="1.04" ScaleY="1.04" />
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Trigger>
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter Property="RenderTransform">
                                                                    <Setter.Value>
                                                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97" />
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource PrimaryActionBrush}" />
                                                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryActionBrush}" />
                                                    <Setter Property="Foreground" Value="{DynamicResource OnPrimaryActionBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <Button.ToolTip>
                                        <StackPanel>
                                            <TextBlock FontWeight="SemiBold" Text="{Binding Title}" />
                                            <TextBlock Margin="0,3,0,0" Text="Tıkla: seç · Sürükle: sırala" />
                                        </StackPanel>
                                    </Button.ToolTip>
                                    <Grid Width="46" Height="45">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="25" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Row="0">
                                            <Image Width="19"
                                                   Height="19"
                                                   Source="{Binding ImagePath}"
                                                   Stretch="Uniform"
                                                   Visibility="{Binding HasImage, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                            <Viewbox Width="19"
                                                     Height="19"
                                                     Visibility="{Binding HasPaths, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <Grid Width="24" Height="24">
                                                    <ItemsControl ItemsSource="{Binding IconPaths}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate><Grid /></ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Path Data="{Binding}"
                                                                      Fill="Transparent"
                                                                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                                      StrokeEndLineCap="Round"
                                                                      StrokeLineJoin="Round"
                                                                      StrokeStartLineCap="Round"
                                                                      StrokeThickness="2" />
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </Grid>
                                            </Viewbox>
                                            <TextBlock HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       FontWeight="Bold"
                                                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                       Text="{Binding FallbackText}"
                                                       Visibility="{Binding HasFallback, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        </Grid>
                                        <TextBlock Grid.Row="1"
                                                   MaxWidth="44"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontSize="8"
                                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                   Text="{Binding Title}"
                                                   TextAlignment="Center"
                                                   TextTrimming="CharacterEllipsis" />
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Canvas>

                <Grid Grid.Row="2" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button MinWidth="84"
                            MinHeight="32"
                            Padding="10,5"
                            Command="{Binding PreviousRingPreviewPageCommand}"
                            Content="← Önceki"
                            Visibility="{Binding HasRingPreviewPages, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <StackPanel Grid.Column="1"
                                Margin="12,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <TextBlock HorizontalAlignment="Center"
                                   FontWeight="SemiBold"
                                   Text="{Binding RingPreviewSelectedTitle}"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Margin="0,2,0,0"
                                   HorizontalAlignment="Center"
                                   Style="{DynamicResource MutedText}"
                                   Text="{Binding RingPreviewSelectedDetails}"
                                   TextAlignment="Center"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                    <Button Grid.Column="2"
                            MinWidth="84"
                            MinHeight="32"
                            Margin="0"
                            Padding="10,5"
                            Command="{Binding NextRingPreviewPageCommand}"
                            Content="Sonraki →"
                            Visibility="{Binding HasRingPreviewPages, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>

                <TextBlock Grid.Row="3"
                           Margin="0,8,0,0"
                           HorizontalAlignment="Center"
                           Style="{DynamicResource MutedText}"
                           Text="Alt+←/→ seçili aksiyonu sıralar."
                           TextAlignment="Center" />
            </Grid>
        </Border>
    </Expander>
</UserControl>
